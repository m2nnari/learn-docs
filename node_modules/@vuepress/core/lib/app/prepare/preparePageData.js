"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.preparePageData = void 0;
const resolvePageData_1 = require("./resolvePageData");
/**
 * Generate page data temp file of a single page
 */
const preparePageData = async (app, page) => {
    // resolve default page data
    const pageData = resolvePageData_1.resolvePageData(page);
    // plugin hook: extendsPageData
    const extendsPageData = await app.pluginApi.hooks.extendsPageData.process(page, app);
    // extends default page data
    extendsPageData.forEach((item) => Object.assign(pageData, item));
    // page data file content
    let content = `export const data = ${JSON.stringify(pageData, null, 2)}\n`;
    // HMR support
    const hmrCode = `
if (import.meta.webpackHot) {
  import.meta.webpackHot.accept()
  __VUE_HMR_RUNTIME__.updatePageData(data)
}
`;
    if (app.env.isDev) {
        content += hmrCode;
    }
    await app.writeTemp(page.dataFilePathRelative, content);
};
exports.preparePageData = preparePageData;
//# sourceMappingURL=preparePageData.js.map