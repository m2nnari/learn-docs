"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createClientBaseConfig = void 0;
const createBaseConfig_1 = require("./createBaseConfig");
const createClientBaseConfig = ({ app, options, isBuild, }) => {
    const config = createBaseConfig_1.createBaseConfig({
        app,
        options,
        isServer: false,
        isBuild,
    });
    // client entry
    config.entry('app').add(app.dir.client('lib/client.js'));
    // client output
    config.output
        .path(app.dir.dest())
        .filename(app.env.isProd
        ? 'assets/js/[name].[chunkhash:8].js'
        : 'assets/js/[name].js')
        .publicPath(app.options.base);
    return config;
};
exports.createClientBaseConfig = createClientBaseConfig;
//# sourceMappingURL=createClientBaseConfig.js.map